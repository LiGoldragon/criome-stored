# criome-stored

The store agent — owns the `blob` relation and provides content-addressed blob
storage. FUSE/socket interface planned, root daemon future.

## VCS

Jujutsu (`jj`) is mandatory. Git is the backend only. Always pass `-m` to
`jj` commands.
